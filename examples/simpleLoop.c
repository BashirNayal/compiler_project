int main() {
    int start_time = get_time();
    int arr_size = 1000;
    int index = 0;
    int arr[arr_size];
    while (index < arr_size) {
        arr[index];
        index = index + 1;
    }
    int end_time = get_time();
    printf("%d\n" , end_time - start_time);

    return 0;
}